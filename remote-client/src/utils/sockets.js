// Deployed remote server
// import dotenv from "dotenv";
// dotenv.config();

// const environment = process.env.NODE_ENV;
// export default socket_url =
//   environment === "production"
//     ? "https://tutorlink-remote-server-802c25e4e6b9.herokuapp.com/"
//     : "http://localhost:3500";

const socket_url = "http://localhost:3500";
export default socket_url;

// setSocket(io("http://localhost:3500")); // Node chat server
